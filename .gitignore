# macOS
.DS_Store
.VSCodeCounter

# Python
.pytest_cache

# Secrets and local configuration (never commit)
/_locals/
/.ssh/

# Buildroot source (cloned separately, not part of this repo)
/buildroot/

# Build outputs (host-side bind mounts, populated at runtime)
/images/
/target/
/graphs/
