# Each BR2_EXTERNAL is an independent repository cloned into this directory.
# .git/info/exclude lists each one (README.md, step 1), not this file, so a
# search from the root of this repository reads them.
